<a href=@{RootR}>トップへ

<h1>スレッド：#{threadTitle $ snd thread}

<form method=post enctype=#{enctype}>
  ^{widget}
  <input type=submit>

<form method=post enctype=#{enctype}>
  ^{widgetForDelete}
  <input type=submit value="このスレッドを削除">

<ul>
  $forall entry <- entries
    <li><a href=@{EntryR $ fromKey $ fst entry}>#{entryContent $ snd entry}
